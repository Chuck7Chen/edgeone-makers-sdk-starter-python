# 复制本文件为 .env 后填写。.env 已在 .gitignore 中，不会被提交。
# Copy this file to .env and fill it in. .env is gitignored.

# EdgeOne Makers API Token。只放服务端，绝不下发给浏览器。
MAKERS_API_TOKEN=

# 可选。留空时 SDK 会自动探测（先 china 再 global），每个 Client 实例探测一次。
# 生产环境建议显式指定，省掉这次探测请求。取值：china | global
# MAKERS_REGION=china
